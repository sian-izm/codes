import { expect } from 'chai'
import { numIslands } from '../src/num-islands'

describe('numIsland', () => {
  // it('should return 1', () => {
  //   expect(numIslands([['1']])).to.equal(1)
  // })
})
